# Changelog

## v1.8.0 - 2026-09-19

- 新增：普通视频下载完成后统一输出为 MP4；必要时由 FFmpeg 转码，确保最终文件扩展名和容器一致。
- 保持：仅音频模式不转成视频 MP4，继续按音频格式输出。
- 新增：macOS 双击脚本 `connect-native-host.command`，首次输入扩展 ID 后可自动注册桥接器，后续运行会复用已保存的 ID。
- 文档：README 增加 macOS 安装教程、桥接排错说明和界面截图。

## v1.7.0 - 2026-09-19

- 移除：删除下载前的实际可用清晰度探测，不再等待网络解析。
- 新增：使用固定清晰度下拉框，支持 2160p、1440p、1080p、720p、480p、360p、最高质量和仅音频，默认 1080p。
- 改进：当视频没有用户选择的分辨率时，由 yt-dlp 自动回退到该上限内可用的格式。

## v1.5.0 - 2026-09-19

- 新增：支持 macOS + Chrome / Edge 的 Native Messaging 注册方式。
- 新增：`setup-native-host.sh` 使用绝对 Python 路径生成启动器，并写入 macOS 浏览器的 `NativeMessagingHosts` 目录。
- 改进：桥接器按当前浏览器读取下载目录，并在 Cookie 回退时使用 Chrome / Edge 对应的浏览器配置。
- 改进：macOS GUI 启动的 Chrome 也能找到 Homebrew 安装的 yt-dlp、Node.js 和 FFmpeg。
- 新增：`build-release.sh` 可在 macOS 上生成干净的扩展 ZIP。

## v1.4.3 - 2026-08-23

- 新增：点击 YouTube 页面下载按钮后，可在弹窗中选择最高质量、2160p、1440p、1080p、720p、480p、360p，或底部的「仅音频」模式。
- 新增：清晰度选择经过后台与本机桥接器白名单校验；仅音频模式只请求 `bestaudio`，不下载视频，也不额外下载字幕。
- 改进：视频下载分别显示视频流和音频流进度；仅音频下载显示单独的音频进度，避免进度百分比互相覆盖。
- 新增：每条媒体流显示实时下载速度和预估剩余时间。
- 改进：YouTube 页面下载按钮保持静态下载图标，不再在按钮上切换状态或显示进度；详细反馈保留在页面提示和扩展任务卡中。

## v1.4.2 - 2026-08-21

- 修复：消除所有可能产生「Uncaught (in promise)」的路径——`popup.js` / `options.js` 中未兜底的 `chrome.runtime.sendMessage`、`background.js` 的 `taskStoreReady` / `publishTask` / `failActiveTasks` 未捕获拒绝，全部补上 try/catch 或 `.catch`。扩展重新加载瞬间出现的「Error: No SW」不再刷出未捕获错误。
- 修复：YouTube 页面间切换后，下载按钮不会重置，仍显示上一视频的绿色「下载完成」状态并可能误触发两次点击；现在导航到其他视频时会自动恢复为默认状态。
- 修复：点击下载后立即禁用按钮，避免桥接响应较慢时重复点击产生重复下载任务。

## v1.4.1 - 2026-08-02

- 安装与打包脚本改为 `.bat`（不再使用 PowerShell 脚本）：`setup-native-host.bat`、`build-release.bat`。
- Release 压缩包固定命名为 `edge-ytdlp-youtube-downloader.zip`（文件名不再附带版本号）。
- `setup-native-host.bat` 支持无参数双击：交互输入扩展 ID，结束时暂停窗口，避免「闪退」看不清报错。
- README 补充「复制扩展 ID → 绑定本机桥接」完整步骤与截图。

## v1.4.0 - 2026-08-02

- 新设备更稳健：安装脚本自动检测并尽量安装 Python / yt-dlp[default] / Node.js / FFmpeg。
- 桥接器支持依赖诊断与「安装缺失工具」；缺少 yt-dlp 时下载前会自动 `pip install`。
- yt-dlp 解析支持 PATH 与 `python -m yt_dlp` 回退，适配 `pip --user` 未进 PATH 的新环境。
- 错误提示改为可操作的中文说明（桥接未注册、机器人检测、格式不可用、代理 TLS、FFmpeg/Node 缺失等）。
- 弹窗与设置页展示依赖状态、安装命令（含当前扩展 ID）和安装进度。
- 新增扩展图标（16/32/48/128）。

## v1.3.3 - 2026-07-27

- 处理扩展重载后旧 YouTube 标签页的 `Extension context invalidated`，避免产生未捕获 Promise 报错。
- 后台通信和页面桥接资源读取统一使用安全调用；检测到旧上下文后停止轮询。

## v1.3.2 - 2026-07-27

- 修复 Windows 中文 yt-dlp 输出被错误解码后在弹窗和按钮提示中显示乱码。
- 下载完成提示改为显示干净的下载目录，不再直接显示可能受控制台编码影响的完整文件路径。

## v1.3.1 - 2026-07-27

- 修复内容脚本隔离环境无法读取 YouTube 播放器当前字幕轨道，新增页面上下文桥接脚本。
- 发布打包文件列表加入 `page-bridge.js`。

## v1.3.0 - 2026-07-27

- 从视频播放页读取 YouTube 播放器当前字幕轨道，仅下载当前语言及当前字幕类型。
- 将字幕下载拆分为视频下载后的独立最佳努力步骤，字幕 HTTP 429/502 不再使视频任务失败。
- 为视频和字幕请求增加 HTTP/分片指数退避，并把 yt-dlp 最后错误写入桥接日志。

## v1.2.1 - 2026-07-27

- 字幕下载语言限制为中文和英文及其语言变体，避免下载过多字幕文件。

## v1.2.0 - 2026-07-27

- 下载视频时自动尝试保存该视频可用的人工字幕和自动生成字幕。
- 字幕按 VTT/SRT 等可用格式保存到与视频相同的下载目录；没有字幕时不影响视频下载。

## v1.1.0 - 2026-07-13

- 在 YouTube 搜索结果中加入逐条下载按钮。
- 在播放列表的每条视频菜单旁加入下载按钮。
- 在视频播放页“保存”和三点菜单之间加入下载按钮。
- 支持 Edge 当前 YouTube Cookie、本机 7890 SOCKS5H 代理和浏览器下载目录。
- 支持并行任务、实时进度、完成路径与详细错误反馈。
- 增加 yt-dlp EJS/Node 支持、网络重试和 PO Token 变化兼容措施。
- 增加 Native Host 安装脚本、诊断日志和干净 Release 打包脚本。
